# Auto-generated by scripts/generate-sam-template.js — do not edit by hand.
# Regenerate: node scripts/generate-sam-template.js
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: EquipTrack REST API (API Gateway), Lambda invoke permissions, optional custom domain — topology from libs/shared/src/api/endpoints.ts

Parameters:
  Stage:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - production
    Description: Deployment stage (matches STAGE in deploy scripts)
  CertificateArn:
    Type: String
    Default: ''
    Description: ACM certificate ARN in the API region for REGIONAL custom domain; leave empty to skip domain resources in this stack (use setup-api-custom-domain.js instead)
  ApiHostname:
    Type: String
    Default: ''
    Description: API hostname (e.g. dev-api.equip-track.com); required with CertificateArn for custom domain in stack
  LambdaCodeBucketName:
    Type: String
    Description: S3 bucket containing the shared Lambda zip (upload before sam deploy)
  LambdaCodeS3Key:
    Type: String
    Description: S3 object key for shared-lambda-bundle.zip (use content hash in key so updates trigger stack changes)
  E2eAuthEnabled:
    Type: String
    Default: 'false'
    AllowedValues:
      - 'true'
      - 'false'
    Description: When true, sets E2E_AUTH_* on all handler Lambdas (dev E2E login)
  E2eAuthSecret:
    Type: String
    Default: ''
    NoEcho: true
    Description: E2E auth secret when E2eAuthEnabled is true

Conditions:
  HasCustomDomain:
    Fn::And:
      - Fn::Not:
          - Fn::Equals:
              - !Ref CertificateArn
              - ''
      - Fn::Not:
          - Fn::Equals:
              - !Ref ApiHostname
              - ''
Resources:
  EquipTrackLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: EquipTrackLambdaDataAccess
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:Query
                  - dynamodb:Scan
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:BatchGetItem
                  - dynamodb:BatchWriteItem
                Resource:
                  - !Sub 'arn:aws:dynamodb:${AWS::Region}:*:table/UsersAndOrganizations*'
                  - !Sub 'arn:aws:dynamodb:${AWS::Region}:*:table/Inventory*'
                  - !Sub 'arn:aws:dynamodb:${AWS::Region}:*:table/Forms*'
                  - !Sub 'arn:aws:dynamodb:${AWS::Region}:*:table/EquipTrackReport*'
                  - !Sub 'arn:aws:dynamodb:${AWS::Region}:*:table/*/index/*'
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource:
                  - !Sub 'arn:aws:secretsmanager:${AWS::Region}:*:secret:equip-track/jwt-private-key*'
                  - !Sub 'arn:aws:secretsmanager:${AWS::Region}:*:secret:equip-track/jwt-public-key*'
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:GetObject
                  - s3:DeleteObject
                Resource:
                  - arn:aws:s3:::equip-track-forms/*
                  - !Sub 'arn:aws:s3:::equip-track-forms-${Stage}/*'

  LambdaaddInventory:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-addInventory-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'addInventory'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdaapproveForm:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-approveForm-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'approveForm'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdacreateForm:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-createForm-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'createForm'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdadeleteProduct:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-deleteProduct-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'deleteProduct'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  Lambdae2eAuth:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-e2eAuth-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'e2eAuth'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetAllForms:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getAllForms-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getAllForms'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetInventory:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getInventory-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getInventory'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetItemsToReport:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getItemsToReport-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getItemsToReport'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetPresignedUrl:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getPresignedUrl-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getPresignedUrl'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetProducts:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getProducts-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getProducts'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetReportsByDates:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getReportsByDates-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getReportsByDates'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetUserForms:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getUserForms-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getUserForms'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetUserInventory:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getUserInventory-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getUserInventory'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagetUsers:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-getUsers-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'getUsers'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdagoogleAuth:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-googleAuth-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'googleAuth'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdainviteUser:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-inviteUser-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'inviteUser'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdapublishPartialReport:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-publishPartialReport-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'publishPartialReport'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdarejectForm:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-rejectForm-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'rejectForm'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdaremoveInventory:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-removeInventory-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'removeInventory'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdasetProduct:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-setProduct-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'setProduct'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  LambdasetUser:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-setUser-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'setUser'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret

  Lambdastart:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'equip-track-start-${Stage}'
      Handler: index.handler
      Runtime: nodejs20.x
      Timeout: 30
      MemorySize: 256
      CodeUri:
        Bucket: !Ref LambdaCodeBucketName
        Key: !Ref LambdaCodeS3Key
      Role: !GetAtt EquipTrackLambdaRole.Arn
      Environment:
        Variables:
          STAGE: !Ref Stage
          LAMBDA_HANDLER_KEY: 'start'
          E2E_AUTH_ENABLED: !Ref E2eAuthEnabled
          E2E_AUTH_SECRET: !Ref E2eAuthSecret


  EquipTrackApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: !Sub 'equip-track-api-${Stage}'
      StageName: !Ref Stage
      EndpointConfiguration: REGIONAL
      Cors:
        AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS'"
        AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token,Accept,Origin,X-Requested-With'"
        AllowOrigin: "'*'"
        AllowCredentials: false
        MaxAge: "'86400'"
      DefinitionBody:
        openapi: 3.0.1
        info:
          title: !Sub 'EquipTrack API ${Stage}'
          version: 1.0.0
        paths:
          /api/auth/e2e-login:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-e2eAuth-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/auth/google:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-googleAuth-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/all-forms:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getAllForms-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/forms/approve:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-approveForm-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/forms/create:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-createForm-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/forms/reject:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-rejectForm-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/forms/user/{userId}/{formId}/presigned-url:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getPresignedUrl-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/inventory:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getInventory-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/inventory/add:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-addInventory-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/inventory/remove:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-removeInventory-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/inventory/user/{userId}:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getUserInventory-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/my-forms:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getUserForms-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/products:
            delete:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-deleteProduct-${Stage}/invocations'
              responses:
                default:
                  description: Default response

            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getProducts-${Stage}/invocations'
              responses:
                default:
                  description: Default response

            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-setProduct-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/reports/by-dates:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getReportsByDates-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/reports/items-to-report:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getItemsToReport-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/reports/publish:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-publishPartialReport-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/users:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-getUsers-${Stage}/invocations'
              responses:
                default:
                  description: Default response

            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-setUser-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/organizations/{organizationId}/users/invite:
            post:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-inviteUser-${Stage}/invocations'
              responses:
                default:
                  description: Default response
          /api/users/start:
            get:
              x-amazon-apigateway-integration:
                type: aws_proxy
                httpMethod: POST
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:equip-track-start-${Stage}/invocations'
              responses:
                default:
                  description: Default response
  LambdaInvokeaddInventory:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdaaddInventory
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokeapproveForm:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdaapproveForm
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokecreateForm:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdacreateForm
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokedeleteProduct:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdadeleteProduct
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokee2eAuth:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref Lambdae2eAuth
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetAllForms:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetAllForms
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetInventory:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetInventory
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetItemsToReport:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetItemsToReport
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetPresignedUrl:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetPresignedUrl
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetProducts:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetProducts
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetReportsByDates:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetReportsByDates
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetUserForms:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetUserForms
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetUserInventory:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetUserInventory
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegetUsers:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagetUsers
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokegoogleAuth:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdagoogleAuth
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokeinviteUser:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdainviteUser
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokepublishPartialReport:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdapublishPartialReport
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokerejectForm:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdarejectForm
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokeremoveInventory:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdaremoveInventory
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokesetProduct:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdasetProduct
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokesetUser:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LambdasetUser
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  LambdaInvokestart:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref Lambdastart
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EquipTrackApi}/*/*'
  ApiCustomDomain:
    Type: AWS::ApiGateway::DomainName
    Condition: HasCustomDomain
    Properties:
      DomainName: !Ref ApiHostname
      RegionalCertificateArn: !Ref CertificateArn
      EndpointConfiguration:
        Types:
          - REGIONAL
      SecurityPolicy: TLS_1_2

  ApiBasePathMapping:
    Type: AWS::ApiGateway::BasePathMapping
    Condition: HasCustomDomain
    DependsOn: EquipTrackApiStage
    Properties:
      DomainName: !Ref ApiCustomDomain
      RestApiId: !Ref EquipTrackApi
      Stage: !Ref Stage

Outputs:
  RestApiId:
    Description: API Gateway REST API ID
    Value: !Ref EquipTrackApi
  ApiUrl:
    Description: Default execute-api URL for this stage
    Value: !Sub 'https://${EquipTrackApi}.execute-api.${AWS::Region}.amazonaws.com/${Stage}'
  CustomDomainUrl:
    Condition: HasCustomDomain
    Description: HTTPS URL when custom domain is managed by this stack
    Value: !Sub 'https://${ApiHostname}'
